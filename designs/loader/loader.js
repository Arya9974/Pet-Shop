const myTimeout = setTimeout(myfunction, 3500);

function myfunction() {
    document.getElementById("Error").innerHTML = "Oh you don't have an account"
  }
  const MyloginTimeout = setTimeout(MyFunction, 4000)
  
  function MyFunction() {
    document.getElementById("Login").innerHTML= "Create an account"
  }